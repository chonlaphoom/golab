# Worker Pool

A simple worker pool implementation example in Go.

## Structure

- `main.go` - Entry point for the worker pool module
- `worker/` - Worker implementation package

## Usage

```bash
go run main.go
```

This module demonstrates the worker pool pattern for concurrent task processing.
